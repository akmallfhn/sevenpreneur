import LoadingStateLMS from "@/app/components/state/LoadingStateLMS";

export default function Loading() {
  return (
    <div className="flex items-center justify-center w-full h-screen lg:pl-64">
      <LoadingStateLMS />
    </div>
  );
}
